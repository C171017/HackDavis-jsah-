# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Mapbox (optional — enables the pantry map)
# Get token at https://console.mapbox.com → Account → Tokens
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_token_here
